const fs = require('fs'); const code = fs.readFileSync('app/admin/page.tsx', 'utf8'); const lines = code.split('\n'); let depth = 0; lines.forEach((line, i) => { const open = (line.match(/<div/g) || []).length; const close = (line.match(/<\/div>/g) || []).length; depth += open - close; if (depth < 0) { console.log('Unmatched close at line ' + (i+1)); depth = 0; } }); console.log('Final depth:', depth);
